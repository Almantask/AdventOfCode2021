namespace AdventOfCode.Day8
{
    public class SevenSegmentDisplayTests
    {
        //  aaaa
        // b    c
        // b    c
        //  dddd
        // e    f
        // e    f
        //  gggg


        public void Test()
        {
            // Submarine display contains 4 digits (0-9).
            // A single display is made of 7 segments (a-g).
            // Segment is a line (bb, aaaa, cc, ...).
            // Segments are connected to wires randomly (separate for each 4 displays).
            // Testing each digit on a display produces a sequence of segments that light up (a-g).

            // And a four digit value. On the 4 screens?

            // Count digits in the output values
            // How many times: 1, 4, 7 and 8 appear?
        }
    }

    public class SevenSegmentDisplay
    {

    }
}
